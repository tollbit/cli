// Package version holds the public CLI and bundled-skill release number.
// Bump this when shipping; keep skill frontmatter `version` in sync (tests enforce it).
package version

const Version = "0.2.2"
